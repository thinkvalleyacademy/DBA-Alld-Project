# ---------- Port Configuration ----------
# Backend API port (maps container 8081 to host 5281)
BACKEND_PORT=5281
# Management port is internal (8090), not exposed to host

# ---------- Backend routing ----------
BACKEND_HOST=api.alld.example.com
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_TLS=true

# ---------- Timezone ----------
TZ=Asia/Kolkata

# ---------- MySQL ----------
MYSQL_ROOT_PASSWORD=change-me-root-pass
MYSQL_DATABASE=dba
MYSQL_USER=rwroot
MYSQL_PASSWORD=change-me-app-pass

# ---------- Spring app secrets ----------
JWT_SECRET=replace-with-at-least-32-characters
AWS_ACCESS_KEY_ID=replace-with-access-key-id
AWS_SECRET_ACCESS_KEY=replace-with-secret-access-key
APP_LOG_DIR=/app/logs
